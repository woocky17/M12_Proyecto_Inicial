{% extends 'base.html.twig' %}

{% block title %}Nurse{% endblock %}

{% block body %}
    <h1>Nurse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ nurse.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ nurse.name }}</td>
            </tr>
            <tr>
                <th>Gmail</th>
                <td>{{ nurse.gmail }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ nurse.password }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_crud_index') }}">back to list</a>

    <a href="{{ path('app_crud_edit', {'id': nurse.id}) }}">edit</a>

    {{ include('crud/_delete_form.html.twig') }}
{% endblock %}
